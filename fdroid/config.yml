# F-Droid custom repository (binary APKs from CI). Published via GitHub Pages.
# Repo URL is overridden in CI from GITHUB_REPOSITORY; default matches OpenBahn-Navigator.
repo_url: https://t-vk.github.io/OpenBahn-Navigator/fdroid/repo
repo_name: OpenBahn Navigator
repo_description: Official OpenBahn Navigator APK repository (FOSS, CI-signed release builds)
repo_maxage: 366
archive_older: 2

# Repo index signing: injected by .github/scripts/prepare-fdroid-signing.sh (see .github/signing/).
# repo_keysha256: added automatically by fdroid update
